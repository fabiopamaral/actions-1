name: Generate ascii
on: push

jobs:
    ascii_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repo
          uses: actions/checkout@v4

        - name: List repo files
          run: ls -ltra

        - name: Executing Shell Script
          run: |
            chomod +x ascii-script.sh
            ./ascii-script.sh